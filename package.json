{
  "name": "tfjs-examples-simple-object-detection",
  "version": "0.1.1",
  "description": "",
  "license": "Apache-2.0",
  "private": true,
  "engines": {
    "node": ">=8.9.0"
  },
  "scripts": {
    "build": "parcel build index.html --public-url ./ --dist-dir dist"
  },
  "devDependencies": {
    "@mediapipe/tasks-vision": "^0.10.22",
    "parcel": "^2.16.4"
  }
}
